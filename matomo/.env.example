# Copy this file to .env and set secure values before running Docker Compose.
# cp .env.example .env

# MySQL root password for internal DB administration
MYSQL_ROOT_PASSWORD=change-this-root-password

# Application DB user used by Matomo
MATOMO_USER=matomo
MATOMO_PASSWORD=change-this-matomo-password
